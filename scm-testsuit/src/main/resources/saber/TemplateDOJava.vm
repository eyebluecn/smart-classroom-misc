/**
 * smart-classroom.com Inc. Copyright (c) 2015-present All Rights Reserved.
 * generated by SaberGenerator
 */
package ${artwork.entityPackage};

#foreach($item in $artwork.entityImportNones)
import ${item};
#if(!$foreach.hasNext)

#end
#end
#foreach($item in $artwork.entityImportJavas)
import ${item};
#if(!$foreach.hasNext)

#end
#end
/**
 * ${artwork.tableRemark}
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
@EqualsAndHashCode(callSuper = true)
public class ${artwork.entityUpperClassName} extends BaseEntityDO {

#foreach($column in $artwork.selfColumns)
    /**
     * ${column.remark}
     */
    private ${column.javaSimpleName} ${column.lowerCamelName} = ${column.defaultValue};

#end

}